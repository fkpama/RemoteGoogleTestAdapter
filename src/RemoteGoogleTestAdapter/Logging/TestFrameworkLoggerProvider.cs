using Microsoft.Extensions.Logging;
using Microsoft.VisualStudio.TestPlatform.ObjectModel.Logging;

namespace RemoteGoogleTestAdapter.Logging
{
    //internal sealed class TestFrameworkLoggerProvider : ILoggerProvider
    //{
    //    private readonly IMessageLogger logger;

    //    internal TestFrameworkLoggerProvider(IMessageLogger logger)
    //    {
    //        this.logger = logger;
    //    }

    //    public ILogger CreateLogger(string categoryName)
    //    {
    //        return new VsTestFrameworkLogger(categoryName, logger);
    //    }

    //    void IDisposable.Dispose() { }
    //}
}
